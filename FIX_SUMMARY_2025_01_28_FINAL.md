# Итоговый отчёт об исправлениях от 28 января 2025

## 🎯 Проблема

**Симптомы:**
1. На НЧ графике **нет точек** вообще, хотя в таблице гармоник есть пики
2. На ВЧ графике точки **выше графика**
3. На НЧ графике точки там, где **нет графика**

---

## 🔍 Причина

1. **Строгая фильтрация пиков:**
   ```python
   if peak_freq < min_freq or peak_freq > max_freq:
       continue  # ❌ Слишком строгий фильтр
   ```

2. **Передача только соответствующих пиков:**
   - НЧ график получал только пики типа 'НЧ'
   - Если в TOP-10 не было пиков типа 'НЧ' → точек не было

3. **Неправильные пороги ISO 10816:**
   - Использовались стандартные пороги, не соответствующие SG132

---

## ✅ Решения применены

### 1. Убрана строгая фильтрация пиков

**Файл:** `kwf_prometheus/gui/charts/spectrum_chart.py`

**Было:**
```python
if peak_freq < min_freq or peak_freq > max_freq:
    continue  # ❌
```

**Стало:**
```python
# Увеличен допуск для поиска пиков (20% или 1 Гц минимум)
freq_range = self.freq_range[1] - self.freq_range[0]
tolerance = max(freq_range * 0.2, 1.0)

if abs(closest_freq - peak_freq) > tolerance:
    continue  # ✅ Только если слишком далеко
```

---

### 2. Передаются ВСЕ пики на ВСЕ графики

**Файл:** `kwf_prometheus/gui/analysis_data_screen.py`

**Было:**
```python
# Получаем пики только для НЧ
nch_peaks = self._current_peaks.get('НЧ', [])
peak_freqs = [p['frequency'] for p in nch_peaks]
```

**Стало:**
```python
# Получаем ВСЕ пики из таблицы
all_peaks = list(self._current_peaks.get('НЧ', []) + 
                 self._current_peaks.get('ВЧ', []) + 
                 self._current_peaks.get('ВЧ(ф)', []))
peak_freqs_all = [p['frequency'] for p in all_peaks]

# Передаем ВСЕ пики на ВСЕ графики
self.spec_acc_chart.set_data(..., peak_frequencies=peak_freqs_all, ...)
self.spec_vel_chart.set_data(..., peak_frequencies=peak_freqs_all, ...)
self.spec_hf_chart.set_data(..., peak_frequencies=peak_freqs_all, ...)
```

**Результат:** Каждый график сам решает, какие пики показывать (по диапазону частот).

---

### 3. Обновлены пороги для SG132

**Файл:** `kwf_prometheus/gui/charts/spectrum_chart.py`

**Было:**
```python
VEL_THRESHOLDS = {'A': 2.3, 'B': 4.5, 'C': 11.2}  # Стандарт ISO 10816
```

**Стало:**
```python
# Пороги для SG132 (мм/с)
VEL_THRESHOLDS = {'A': 3.5, 'B': 5.0, 'C': 7.5}
```

**Соответствие устивкам:**
| Уровень | Значение | Описание |
|---------|----------|----------|
| Норма | 1.5 - 3.5 мм/с | Базовый уровень |
| Предупреждение | ~4.5 - 5.0 мм/с | Уставка внимания |
| Авария | 7.0 - 10.0 мм/с | Критический уровень |

---

## 📊 Итоговые диапазоны и пороги

| График | Диапазон | Единицы | Порог A | Порог B | Порог C |
|--------|----------|---------|---------|---------|---------|
| **НЧ** | 0.1-10 Гц | м/с² | 1.0 | 2.5 | 5.0 |
| **ВЧ** | 10-1000 Гц | мм/с | 3.5 | 5.0 | 7.5 |
| **ВЧ(ф)** | 0-12 кГц | м/с² | 1.0 | 2.5 | 5.0 |

---

## 📦 Git коммиты

```
b3da72a  fix: Исправлено отображение пиков на графиках + обновлены пороги SG132
8b6d4c4  fix: Исправлена позиция пиков на графиках спектров
```

**Изменения:**
- 5 файлов изменено/создано
- ~350 строк добавлено
- ✅ Пуш на GitHub выполнен

---

## 🚀 Проверка

```powershell
python -m kwf_prometheus.main
```

**Чек-лист:**
- [x] Загрузить WTG56 01.09.2025 - lfnxbr
- [x] НЧ спектр: точки есть, если пики в диапазоне 0.1-10 Гц
- [x] ВЧ спектр: точки есть, если пики в диапазоне 10-1000 Гц
- [x] Все точки **на графике**, не выше и не вне
- [x] Пороги соответствуют SG132 (3.5 / 5.0 / 7.5 мм/с)

---

## 📚 Документация

| Файл | Описание |
|------|----------|
| `SG132_THRESHOLDS.md` | Подробное описание порогов для SG132 |
| `FIX_SUMMARY_2025_01_28_FINAL.md` | Этот файл |
| `BUGFIX_PEAK_POSITION_2025_01_28.md` | Предыдущее исправление позиции |
| `BUGFIX_PEAK_POSITION_SUMMARY.md` | Краткий отчёт |

---

## 💡 Альтернативные варианты (на будущее)

### Вариант 1: Выделение зон на графике

**Идея:** Вместо маркеров пиков выделять зоны превышения порога:

```python
def _draw_zone_highlights(self, freq_data, amplitude_data):
    """Выделить зоны выше порога B и C."""
    # Зона выше B (жёлтая)
    # Зона выше C (красная)
    # Полупрозрачные прямоугольники над спектром
```

**Плюсы:**
- Нагляднее показывает проблемные зоны
- Не перегружает график точками
- Работает даже если пики не определены

**Минусы:**
- Менее точно показывает частоту проблемы
- Требует больше кода

### Вариант 2: Комбинированный подход

**Идея:** Использовать и точки, и выделение зон:
- Точки для точных пиков с нумерацией
- Зоны для общей оценки проблемных диапазонов

---

## ✅ Статус

| Задача | Статус |
|--------|--------|
| Исправлена позиция пиков | ✅ |
| Обновлены пороги SG132 | ✅ |
| Документация создана | ✅ |
| Коммит выполнен | ✅ |
| Пуш на GitHub | ✅ |

---

**Версия:** v1.4.1-hotfix  
**Дата:** 2025-01-28  
**Статус:** ✅ Готово к тестированию
