# Отладка отображения пиков на графиках

## 📅 Дата: 2025-01-28

---

## 🐛 Проблема

**Симптом:**
- На ВЧ спектре (10-1000 Гц) точки пиков рисуются за пределами данных (например, на 200 Гц, когда спектр заканчивается на 60-70 Гц)

**Причина:**
- Частота дискретизации ВЧ сигнала (LOW фильтр) может быть низкой (например, 128 Гц)
- Максимальная частота в спектре = `fs/2` = 64 Гц
- Но пики из таблицы могут быть на частотах 200 Гц, 500 Гц и т.д.
- Раньше точки рисовались на этих частотах, хотя данных спектра там нет

---

## ✅ Решение

### 1. Проверка на максимальную частоту данных

```python
# Получаем максимальную частоту в данных
max_data_freq = np.max(freq_data)

# Пик не должен быть дальше максимальной частоты данных
if peak_freq > max_data_freq * 1.1:  # 10% запас
    continue  # Пик вне диапазона данных, пропускаем
```

### 2. DEBUG вывод

```python
print(f"[DEBUG] SpectrumChart: range={self.freq_range}, data_freq_range=({min_data_freq:.1f}-{max_data_freq:.1f} Hz), peaks={len(peak_frequencies)}")
```

---

## 🔍 Как проверить

### Запустить приложение
```powershell
python -m kwf_prometheus.main
```

### Открыть консоль и посмотреть DEBUG вывод

**Ожидаемый вывод для ВЧ спектра:**
```
[DEBUG] SpectrumChart: range=(10, 1000), data_freq_range=(10.0-64.0 Hz), peaks=10
[DEBUG] Skipping peak 2 at 200.0 Hz (max data freq: 64.0 Hz)
[DEBUG] Skipping peak 7 at 1500.0 Hz (max data freq: 64.0 Hz)
```

**Что это значит:**
- Диапазон графика: 10-1000 Гц
- Фактические данные: 10-64 Гц (из-за частоты дискретизации 128 Гц)
- Пики на 200 Гц и 1500 Гц пропускаются, так как данных там нет

---

## 🎯 Ожидаемое поведение

### НЧ спектр (0.1-10 Гц)
- Данные: 0.1-10 Гц
- Пики: только в диапазоне 0.1-10 Гц
- **Ожидаемо:** точки есть на графике

### ВЧ спектр (10-1000 Гц)
- Данные: 10-64 Гц (или меньше, зависит от fs)
- Пики: только в диапазоне 10-64 Гц
- **Ожидаемо:** точки ТОЛЬКО там, где есть данные

### ВЧ(ф) спектр (0-12 кГц)
- Данные: 0-6 кГц (или больше, зависит от fs)
- Пики: в диапазоне 0-6 кГц
- **Ожидаемо:** точки ТОЛЬКО там, где есть данные

---

## 💡 Варианты улучшения

### Вариант 1: Автоматическое масштабирование графика

**Идея:** Автоматически подстраивать диапазон X графика под фактические данные

```python
# В set_data()
if len(freq_filtered) > 0:
    actual_max_freq = np.max(freq_filtered)
    self.plot_widget.setXRange(self.freq_range[0], actual_max_freq * 1.1)
```

**Плюсы:**
- График всегда показывает все данные
- Точки не рисуются в пустоте

**Минусы:**
- Непостоянный масштаб (может путать при сравнении)

### Вариант 2: Уведомление о ограничении частоты

**Идея:** Показывать предупреждение, если пики не отображаются из-за ограничения частоты

```python
if skipped_peaks > 0:
    print(f"[WARNING] {skipped_peaks} пиков не отображены из-за ограничения частоты ({max_data_freq} Гц)")
```

### Вариант 3: Выделение зон превышения (альтернатива точкам)

**Идея:** Вместо точек рисовать полупрозрачные прямоугольники над зонами выше порога

```python
def _draw_zone_highlights(self, freq_data, amplitude_data, threshold):
    """Выделить зоны выше порога."""
    mask = amplitude_data > threshold
    # Рисовать прямоугольник над каждой непрерывной областью
```

---

## 📊 Частота дискретизации и максимальная частота

| Тип сигнала | Фильтр | Частота дискретизации (fs) | Макс. частота (fs/2) |
|-------------|--------|---------------------------|---------------------|
| **НЧ** | FILTER (0.1-10 Гц) | 256 Гц | 128 Гц |
| **ВЧ** | LOW (10-1000 Гц) | 128 Гц | 64 Гц |
| **ВЧ(ф)** | HIGH (0-12 кГц) | 25600 Гц | 12800 Гц |

**Теорема Котельникова-Найквиста:**
> Для корректной регистрации сигнала частота дискретизации должна быть как минимум в 2 раза выше максимальной частоты в сигнале.

---

## ✅ Статус

| Задача | Статус |
|--------|--------|
| Добавлена проверка max_data_freq | ✅ |
| Добавлен DEBUG вывод | ✅ |
| Пики рисуются только в данных | ✅ |
| Коммит выполнен | ✅ |
| Пуш на GitHub | ✅ |

---

**Версия:** v1.4.1-debug  
**Дата:** 2025-01-28  
**Статус:** ✅ Готово к тестированию
