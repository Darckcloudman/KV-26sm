# Исправление позиции пиков на графиках

## 📅 Дата: 2025-01-28

---

## ❌ Описание проблемы

**Симптомы:**
- На ВЧ спектре одна из точек **выше графика**
- На НЧ спектре точки расположены **там, где нет графика**
- Красные точки не соответствуют реальным пикам на спектре

**Причина:**
1. **Диапазоны частот графиков не соответствовали названиям:**
   - НЧ спектр: было `(0, 30)` Гц, должно быть `(0.1, 10)` Гц
   - ВЧ спектр: было `(0, 1200)` Гц, должно быть `(10, 1000)` Гц

2. **Пики фильтровались по неправильным маскам:**
   - `mask = freqs <= 30` вместо `mask = (freqs >= 0.1) & (freqs <= 10)`
   - `mask = freqs <= 1200` вместо `mask = (freqs >= 10) & (freqs <= 1000)`

3. **Пики вне диапазона графика всё равно отображались:**
   - Не было проверки `if peak_freq < min_freq or peak_freq > max_freq`

---

## ✅ Решение

### 1. Исправлены диапазоны частот графиков

**Файл:** `kwf_prometheus/gui/analysis_data_screen.py`

**Было:**
```python
self.spec_acc_chart = SpectrumChart(
    'НЧ спектр (0.1-10 Гц)', 'Гц', 'м/с²', (0, 30),  # ❌ Неправильно
    thresholds=ACC_THRESHOLDS
)
self.spec_vel_chart = SpectrumChart(
    'ВЧ спектр (10-1000 Гц)', 'Гц', 'мм/с', (0, 1200),  # ❌ Неправильно
    thresholds=VEL_THRESHOLDS
)
```

**Стало:**
```python
self.spec_acc_chart = SpectrumChart(
    'НЧ спектр (0.1-10 Гц)', 'Гц', 'м/с²', (0.1, 10),  # ✅ Правильно
    thresholds=ACC_THRESHOLDS
)
self.spec_vel_chart = SpectrumChart(
    'ВЧ спектр (10-1000 Гц)', 'Гц', 'мм/с', (10, 1000),  # ✅ Правильно
    thresholds=VEL_THRESHOLDS
)
```

---

### 2. Исправлены маски фильтрации данных

**Было:**
```python
# НЧ спектр
mask = freqs <= 30  # ❌

# ВЧ спектр
mask = freqs <= 1200  # ❌
```

**Стало:**
```python
# НЧ спектр (0.1-10 Гц)
mask = (freqs >= 0.1) & (freqs <= 10)  # ✅

# ВЧ спектр (10-1000 Гц)
mask = (freqs >= 10) & (freqs <= 1000)  # ✅

# ВЧ(ф) спектр (0-12 кГц)
mask = (freqs >= 0) & (freqs <= 12000)  # ✅
```

---

### 3. Добавлена фильтрация пиков по диапазону графика

**Файл:** `kwf_prometheus/gui/charts/spectrum_chart.py`

**Добавлено в `_add_peak_markers()`:**
```python
# Фильтруем пики по диапазону частот графика
min_freq = self.freq_range[0]
max_freq = self.freq_range[1]

for i, peak_freq in enumerate(peak_frequencies[:10]):
    # Пропускаем пики вне диапазона частот графика
    if peak_freq < min_freq or peak_freq > max_freq:
        continue  # ✅ Пик вне диапазона - не отображаем

    # Ищем ближайшее значение частоты в данных
    closest_idx = np.argmin(np.abs(freq_data - peak_freq))
    
    # Проверяем, что найденная частота действительно близка к пику
    if abs(freq_data[closest_idx] - peak_freq) > (max_freq - min_freq) * 0.1:
        continue  # ✅ Пик слишком далеко от реальных данных
    
    peak_amp = amplitude_data[closest_idx]
    
    # ... создаём маркер ...
```

---

## 📊 Итоговые диапазоны

| График | Название | Диапазон частот | Маска фильтрации |
|--------|----------|-----------------|------------------|
| **НЧ** | 0.1-10 Гц | `(0.1, 10)` Гц | `(freqs >= 0.1) & (freqs <= 10)` |
| **ВЧ** | 10-1000 Гц | `(10, 1000)` Гц | `(freqs >= 10) & (freqs <= 1000)` |
| **ВЧ(ф)** | 0-12 кГц | `(0, 12000)` Гц | `(freqs >= 0) & (freqs <= 12000)` |

---

## ✅ Результат

| Было | Стало |
|------|-------|
| ❌ Точки выше графика | ✅ Все точки на графике |
| ❌ Точки там где нет графика | ✅ Точки только в диапазоне |
| ❌ Неправильные диапазоны | ✅ Диапазоны соответствуют названиям |
| ❌ Пики вне диапазона отображались | ✅ Пики фильтруются по диапазону |

---

## 🎯 Пример работы

**До исправления:**
```
НЧ спектр (0.1-10 Гц):
- Диапазон графика: 0-30 Гц
- Пик 500 Гц (из таблицы) → Отображается на графике ❌

ВЧ спектр (10-1000 Гц):
- Диапазон графика: 0-1200 Гц
- Пик 5 Гц (из таблицы) → Отображается на графике ❌
```

**После исправления:**
```
НЧ спектр (0.1-10 Гц):
- Диапазон графика: 0.1-10 Гц
- Пик 500 Гц → Пропускается (вне диапазона) ✅
- Пик 5 Гц → Отображается корректно ✅

ВЧ спектр (10-1000 Гц):
- Диапазон графика: 10-1000 Гц
- Пик 5 Гц → Пропускается (вне диапазона) ✅
- Пик 500 Гц → Отображается корректно ✅
```

---

## 📝 Изменения в файлах

| Файл | Изменения |
|------|-----------|
| `kwf_prometheus/gui/analysis_data_screen.py` | Исправлены диапазоны графиков и маски фильтрации |
| `kwf_prometheus/gui/charts/spectrum_chart.py` | Добавлена фильтрация пиков по диапазону |

---

## ✅ Проверка

```powershell
cd D:\Coding\pyton_pro
python -c "from kwf_prometheus.gui.charts.spectrum_chart import SpectrumChart; print('[OK]')"
python -c "from kwf_prometheus.gui.analysis_data_screen import AnalysisDataScreen; print('[OK]')"
```

**Результат:** ✅ Все импорты работают корректно

---

**Статус:** ✅ Исправлено  
**Версия:** v1.4.1  
**Приложение:** Готово к тестированию
