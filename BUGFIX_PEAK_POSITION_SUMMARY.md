# Итоги исправления позиции пиков

## 📅 Дата: 2025-01-28

---

## ❌ Проблема (скриншоты)

**Симптомы:**
1. На ВЧ спектре точка **выше графика**
2. На НЧ спектре точки там, где **нет графика**
3. Красные точки не соответствуют пикам

---

## 🔍 Причина

1. **Неправильные диапазоны частот графиков:**
   - НЧ: было `(0, 30)` Гц вместо `(0.1, 10)` Гц
   - ВЧ: было `(0, 1200)` Гц вместо `(10, 1000)` Гц

2. **Неправильные маски фильтрации:**
   - `mask = freqs <= 30` вместо `mask = (freqs >= 0.1) & (freqs <= 10)`

3. **Отсутствие проверки пиков:**
   - Пики вне диапазона всё равно отображались

---

## ✅ Исправления

### 1. Диапазоны частот

```python
# Было:
self.spec_acc_chart = SpectrumChart(..., (0, 30), ...)
self.spec_vel_chart = SpectrumChart(..., (0, 1200), ...)

# Стало:
self.spec_acc_chart = SpectrumChart(..., (0.1, 10), ...)
self.spec_vel_chart = SpectrumChart(..., (10, 1000), ...)
```

### 2. Маски фильтрации

```python
# Было:
mask = freqs <= 30

# Стало:
mask = (freqs >= 0.1) & (freqs <= 10)
```

### 3. Фильтрация пиков

```python
# Добавлено в _add_peak_markers():
if peak_freq < min_freq or peak_freq > max_freq:
    continue  # Пропускаем пики вне диапазона
```

---

## 📦 Git коммит

```
8b6d4c4  fix: Исправлена позиция пиков на графиках спектров
```

**Изменения:**
- 3 файла изменено
- 207 строк добавлено
- 9 строк удалено

---

## ✅ Результат

| Было | Стало |
|------|-------|
| ❌ Точки выше графика | ✅ Все точки на графике |
| ❌ Точки вне диапазона | ✅ Только в диапазоне |
| ❌ Неправильные диапазоны | ✅ Соответствуют названиям |

---

## 🚀 Тестирование

```powershell
python -m kwf_prometheus.main
```

**Чек-лист:**
- [ ] Загрузить WTG56 01.09.2025 - lfnxbr
- [ ] Проверить НЧ спектр (0.1-10 Гц)
- [ ] Проверить ВЧ спектр (10-1000 Гц)
- [ ] Все точки должны быть **на графике**

---

**Статус:** ✅ Исправлено и запушено  
**Версия:** v1.4.1-hotfix
